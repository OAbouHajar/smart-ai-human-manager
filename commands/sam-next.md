---
description: Save the session with an explicit next-session todo list
---

Execute the exact workflow defined in the sibling `wrap-with-next.md` command. `/sam-next` and `/sam-handoff` intentionally perform the same workflow. Do not merely describe or redirect to another command.
