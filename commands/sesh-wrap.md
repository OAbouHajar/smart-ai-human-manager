---
description: Save the current session and update its linked project
---

Execute the exact workflow defined in the sibling `wrap.md` command. This is the preferred Sesh command for that workflow. Do not merely describe or redirect to the old command.
